# P3 — C4 Diagrams

## C1 — System Context

```mermaid
flowchart LR
  %% TODO: external users + external systems + the TaskFlow box
```

## C2 — Containers

```mermaid
flowchart LR
  %% TODO: SPA, API, SignalR, SQL, Redis, Blob, jobs container, with protocol-labelled arrows
```

## Architecture Narrative

> TODO — ~200 words explaining why each container exists, how they communicate, and which ones are deployable units.
