/*
    P2 -- Column Rules: Count, Type, Naming, ORDER BY  (Easy)
    See ../Practice-Problems.md for full requirements.

    1. Deliberately violate the "same column count" rule -- capture the exact error.
    2. UNION app.Users.UserId (INT) with app.Projects.Budget (DECIMAL(12,2)) -- which type wins?
    3. Alias columns differently on each leg of a UNION ALL -- confirm which alias survives.
    4. Put ORDER BY inside the first leg (not at the very end) -- capture the exact error, then fix it.
*/
USE TaskFlowDb;
GO

-- TODO: your solution here
