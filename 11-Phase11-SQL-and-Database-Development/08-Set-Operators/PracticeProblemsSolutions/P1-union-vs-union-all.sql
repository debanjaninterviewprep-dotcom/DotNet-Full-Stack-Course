/*
    P1 -- UNION vs UNION ALL  (Easy)
    See ../Practice-Problems.md for full requirements.

    1. Stack app.Users.Email with a NULL placeholder per leaderless app.Teams.TeamName, using UNION ALL.
    2. Same query with plain UNION -- did the row count change? Why or why not?
    3. Find a genuine case where UNION (not UNION ALL) changes the row count. Show both, with counts.
    4. State the rule for when UNION ALL is safe to default to.
*/
USE TaskFlowDb;
GO

-- TODO: your solution here
