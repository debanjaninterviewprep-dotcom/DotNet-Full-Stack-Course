# P2 — Functional Dependency Analysis

See [Practice-Problems.md](../Practice-Problems.md) P2 for the full requirement list.

## TODO: your analysis here

1. **`app.TaskAssignments (TaskId, UserId, AssignedOn, IsPrimary)`** — list every functional
   dependency and state the candidate key.

2. **Flat `TaskReport` table (from P1)** — identify at least one partial dependency and one
   transitive dependency, written as `X → Y`.

3. **`app.Users`** — why is `Email` a candidate key even though `UserId` is the primary key?
   Name the constraint that enforces it.

4. **`app.TeamMembers`** — define "prime attribute" / "non-prime attribute" in your own words,
   then classify every column.
